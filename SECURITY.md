# 安全策略

“照做”能捕获并回放桌面操作，因此安全问题可能影响真实账号、文件和外部通信。

## 支持范围

当前仅维护 `main` 分支的最新版本。v0.1.x 是技术预览版，不承诺生产级安全或向后兼容。

## 私下报告漏洞

请优先使用 GitHub 仓库的 **Private vulnerability reporting**。不要在公开 Issue、Discussion、录屏或 Pull Request 中披露以下内容：

- 可绕过执行授权或最终副作用确认的方法；
- 能使遮蔽文本、令牌、Cookie 或截图隐私泄露的方法；
- 能诱导执行器跨窗口、跨账号或操作错误目标的方法；
- 未经授权的远程执行、持久化或任意文件访问；
- 包含真实个人数据的复现材料。

报告中请提供受影响版本、最小复现、预期影响和建议缓解方式。请先使用虚构账号和隔离环境验证。

如果 Private vulnerability reporting 暂不可用，请只创建一个不含利用细节的公开 Issue，请求维护者开启私下沟通渠道。

## 不属于安全承诺的内容

档案在未声明的软件版本、DPI、语言或窗口状态下失效，通常属于兼容性问题；但如果失效会绕过确认、泄露隐私或把副作用施加到错误目标，请按安全漏洞报告。
