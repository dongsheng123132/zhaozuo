# Profile Builder（档案提炼器）

本目录预留给“多次示范 → 候选 Shadow Profile”的提炼过程。

第一版不让模型直接把录屏变成可执行脚本。流程是：

1. 对齐同一任务的 2～3 次录制；
2. 找出每次都出现的稳定语义步骤；
3. 把坐标替换为 UIA、快捷键、OCR 或窗口相对定位；
4. 把变化值提取成动作输入；
5. 明确前置条件、失败分支、风险、确认策略和成功证据；
6. 生成候选 Profile，经人工审阅和沙箱回放后才能标记为 `validated`。

未来这里会加入：录制对齐、候选定位器评分、Profile 生成和回归样本编译。
